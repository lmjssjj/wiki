```
//在NotificationManagerService的onStart方法中初始化
com.android.server.notification.NotificationManagerService#onStart
//应用通知保存文件
com.android.server.notification.NotificationManagerService#init(
 	new AtomicFile(new File(
                        systemDir, "notification_policy.xml"), "notification-policy")
)

```

